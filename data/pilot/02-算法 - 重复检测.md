# 重复检测：HashSet 的应用

> 来源：算法每日一题 第 1 期 · 2026-06-23

---

## 问题

给定一个数组，判断里面有没有重复元素。

**场景**：检查粉丝列表有没有重复关注。

---

## 怎么想

最直接的办法：拿一个本子，一个一个看。看到一个新的就记下来。如果看到一个"本子上已经有了"的 → 有重复。

这个"本子"在编程里叫 **HashSet（哈希集合）**——它的特点是：存进去的东西，查"有没有"只要一瞬间。

---

## Python 实现

```python
def contains_duplicate(nums):
    seen = set()
    for num in nums:
        if num in seen:
            return True
        seen.add(num)
    return False

# 测试
print(contains_duplicate([1, 2, 3, 1]))     # True（1 重复了）
print(contains_duplicate([1, 2, 3, 4]))     # False（都不重复）
print(contains_duplicate([526, 86, 526]))   # True（526 重复）
```

---

## 复杂度

- 时间复杂度：O(n) —— 只需要遍历一次数组
- 空间复杂度：O(n) —— 最坏情况下需要存下所有元素

**为什么不用暴力法？** 暴力法拿每个元素跟后面的比，时间复杂度是 O(n²)。1000 个元素要比较 100 万次。HashSet 只要 1000 次。

这就是"空间换时间"。

---

*测试文件 · Neural Garden Pilot Dataset*
