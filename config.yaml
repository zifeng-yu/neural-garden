# Neural Garden 配置文件

dashscope:
  api_key: "your-api-key-here"
  embedding_model: "text-embedding-v3"
  llm_model: "qwen-plus"

chroma:
  persist_directory: "./data/chroma"

pilot_dataset:
  path: "./data/pilot"
